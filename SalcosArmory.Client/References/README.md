These assemblies are compile-time references from the SPT 4.1.2 client environment.
They are not copied into the plugin output (`Private=false`) because SPT provides them at runtime.

`ItemComponent.Types.dll` is required to compile the strongly typed armor-component checks used by field repair.
